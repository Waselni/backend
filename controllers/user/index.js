const createUser = require('./create');
const createToken = require('./token');
const deleteUser = require('./delete');
const getUser = require('./getUser');

module.exports = { createUser, createToken, deleteUser, getUser };
